name: Create development builds

on:
  push:
    branches: ['develop']

jobs:
  android_development_build:
    name: Build Android
    type: build
    workingDirectory: apps/frontend/mobile
    params:
      platform: android
      profile: development
